import { CellRenderDirective } from './cell-render.directive';

describe('CellRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new CellRenderDirective();
    expect(directive).toBeTruthy();
  });
});
