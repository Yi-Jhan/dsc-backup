import { CssHeightPipe } from './css-height.pipe';

describe('CssHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new CssHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
